# Base images downloaded (~900 MB) — the script fetches and verifies them
dl/

# Build disks (tens of GB)
vm/
build-test/

# The distributable image: 6.5 GB, doesn't fit in a repository.
# Published as a release. The README and the sha256 are versioned.
dist/*.zip
dist/*.utm/

# Run output
logs/
provision/*.iso
provision/repair-iso/*.log

# Only the two screenshots the documentation uses are versioned; the rest
# are debugging snapshots (39 MB) that add nothing for someone cloning the repo.
shots/*
!shots/hires.png
!shots/dist-final.png

# macOS
.DS_Store

# Drafts of messages for the Omarchy discussions
drafts/

# v2 build directory
build-v2/
build-v3/
build-v4/
build-v5/
