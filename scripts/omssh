#!/bin/bash
# Shortcut for SSH access into the VM during development.
#   OM_HOST=192.168.64.20 OM_USER=builder ./scripts/omssh "command"
# You can get the IP with:  utmctl ip-address "Omarchy ARM"
: "${OM_USER:=builder}"
: "${OM_HOST:?OM_HOST is required (the VM IP address)}"
exec ssh -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
        -o LogLevel=ERROR -o ConnectTimeout=10 "$OM_USER@$OM_HOST" "$@"
